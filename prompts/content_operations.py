"""内容运营 Prompt 模板。

输出分两部分：面向人阅读的 Markdown 叙述 + 末尾一个 ```json 代码块（供程序渲染图表）。
"""

CONTENT_OPERATIONS_PROMPT = """
你是一位资深的海外社区内容运营专家。以下是从 Reddit 社区 r/{subreddit} 抓取的用户讨论数据。

品牌名称：{user_brand}
目标受众：{target_audience}

请基于社区数据输出一份内容运营洞察报告，先写叙述正文，再在最后附一段结构化 JSON。

【输出格式硬性要求】
- 直接从「## 1. 热门话题 Top 5」开始写正文，**不要**输出“第一部分/第二部分/结构化数据”之类的分节标题或 === 分隔线。
- 正文写完 5 个小节后，**另起一行直接输出一个 ```json 代码块**；代码块前后不要添加任何标题、说明文字或分隔线。

正文小节（全部用中文）：

## 1. 热门话题 Top 5
按热度排序，列出社区讨论最多的 5 个话题方向，每个说明为什么热，并尽量引用数据佐证。

## 2. 用户常见问题与困惑
列出用户反复提出、品牌可通过内容来回答的问题。

## 3. 高互动内容特征
分析高 score / 高评论帖子的共同特征。**每个特征要具体、可量化**（例如“提问式标题”“教程/干货类”“含个人经历”），而不是泛泛而谈。

## 4. 内容选题建议（5-10 条）
每条建议用统一结构给出，便于直接执行：
- **建议标题**：……
- **内容角度**：……
- **目标**：引流 / 建立信任 / 促进转化（三选一）

## 5. 社区参与策略
建议品牌如何以自然、不引起反感的方式参与社区讨论。

正文之后，输出**一个** ```json 代码块，字段严格如下（数值必须来自上面的数据，
禁止凭空捏造；数据不足时给出保守估计，宁可留空数组）：

```json
{{
  "hot_topics": [{{"topic": "话题简述", "heat": 85}}],
  "engagement_features": [{{"feature": "提问式标题", "count": 8}}]
}}
```

字段说明：
- hot_topics：Top5 热门话题，topic 为简短短语（≤15 字），heat 为 0-100 的相对热度整数。
- engagement_features：高互动帖的共同特征，feature 为简短标签，count 为该特征在高互动帖中出现的大致次数（整数）。

要求：
- 全程使用中文。
- 结论尽量引用数据，不足之处明确写“不足以判断”。
- JSON 必须是合法可解析的，且只出现一次。

---
以下是社区数据：
{data_summary}
""".strip()
